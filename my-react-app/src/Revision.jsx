//functions must have a return value
//let and const block scope
//var global scope
//object uses comma instead of semicolon
//in react any css property must be passed as camel case
//align-items should be alignItems
//we put curly braces to signify it is javascript
//class is a reserved keyword so we use className
//React Hooks uses Function Components
//Inline styles in Css are written as objects
//render list=map
//key is a unique word
//SPA=single page applications
//SPA smoother,app-like feel
//Node.js run time it helps us to run JS outside the browser
//Node js provides non blocking and event driven system
//it uses asynchronous programming and promises and call backs and retime projects
//it uses V8 Engine
//it uses request and response arguments
//300 redirect
//404 notFound
//we have 3 operations create(post),read(get)(retrieve of info),update(put /patch) and delete (delete method)
//REPL=Read,Evaluate, Print,Loop
//we define variables with var,let,const
//ARRAY METHODS
//push(add to last element),pop(remove last element),shift(add to first element),unshift(remove firstElement),
// map(looping),filter(delte operations and search)
//.......
//object has key and value pairs and each of them are spearated by comma
//types of function
//name,anonymous(function inside of another function),arrow
//named functions is reusable
//closure=a function housing another function e.g like puttng a function inside another function
//We can write our code in either common js or ES modules
// default  exportmodules.exports={
 // add,
  //Subtract
//}
//import const math=require(./math)
//how to create 4 different expenses e.g math.mjs